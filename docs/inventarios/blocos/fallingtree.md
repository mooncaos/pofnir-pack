# Blocos — FallingTree (`fallingtree`)

Fonte: jar `FallingTree-1.20.1-4.3.4.jar`. **0 blocos** (sem `assets/*/blockstates/`, sem
`data/`, sem `block.fallingtree.*` no lang).

O mod não adiciona bloco: age sobre os troncos e folhas existentes por listas de config
(`trees.allowedLogs`, `deniedLogs`, `allowedLeaves`, `deniedLeaves`, `allowedNonDecayLeaves`,
`allowedAdjacentBlocks`), que aceitam ids e tags `#minecraft:logs` / `#minecraft:leaves`.
